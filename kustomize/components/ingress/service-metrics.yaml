# yaml-language-server: $schema=
kind: Service
apiVersion: v1
metadata:
  name: metrics
  labels:
    app.kubernetes.io/name: traefik
spec:
  selector:
    app.kubernetes.io/name: traefik
  ports:
    - name: metrics
      port: 9100
