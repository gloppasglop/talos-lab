apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: alertmanager
spec:
  dnsNames:
    - alertmanager.example.com
  issuerRef:
    kind: ClusterIssuer
    name: default-issuer
  secretName: alertmanager-cert-tls
