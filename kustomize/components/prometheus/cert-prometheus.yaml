apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus
spec:
  dnsNames:
    - prometheus.example.com
  issuerRef:
    kind: ClusterIssuer
    name: default-issuer
  secretName: prometheus-cert-tls
