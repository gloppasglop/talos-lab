---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: apps.glop.pasglop
  namespace: infra-ingress
spec:
  dnsNames:
    - "*.apps.glop.pasglop"
  secretName: apps-glop-pasglopcert-tls
  issuerRef:
    name: default-issuer
    kind: ClusterIssuer
